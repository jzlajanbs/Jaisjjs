game:GetService("Players").LocalPlayer.Idled:Connect(function()
    game:GetService("VirtualUser"):Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    wait(1)
    game:GetService("VirtualUser"):Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
end)

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/3345-c-a-t-s-u-s/Harmony/refs/heads/main/src/init.luau"))()

local Window = Library.new({
    Title = "FusionX Free Version",
    Scale = UDim2.new(0, 550, 0, 400),
})

local tabAutomation = Window:AddTab({
    Title = "Inicio",
    Icon = "home"
})

local ExampleSection = tabAutomation:AddSection({
    Title = "Configurações"
})

local selectedSpeed = 125
local selectedSize = 2

local speedTextbox = ExampleSection:AddTextbox({
    Title = "Velocidade",
    Placeholder = "Digite a Velocidade...",
    Default = "125",
    Finished = true,
    Callback = function(val)
        selectedSpeed = tonumber(val) or 125
        if getgenv().AutoSpeed and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = selectedSpeed
        end
    end
})

local speedToggle = ExampleSection:AddToggle({
    Title = "Colocar Velocidade Selecionada",
    Default = false,
    Callback = function(state)
        getgenv().AutoSpeed = state
        while getgenv().AutoSpeed do
            if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
                game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = selectedSpeed
            end
            task.wait()
        end
    end
})

game.Players.LocalPlayer.CharacterAdded:Connect(function(char)
    if getgenv().AutoSpeed then
        local humanoid = char:WaitForChild("Humanoid")
        humanoid.WalkSpeed = selectedSpeed
    end
end)

local sizeTextbox = ExampleSection:AddTextbox({
    Title = "Tamanho",
    Placeholder = "Digitar Tamanho...",
    Default = "2",
    Finished = true,
    Callback = function(val)
        selectedSize = tonumber(val) or 2
        if getgenv().AutoSize then
            game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", selectedSize)
        end
    end
})

local sizeToggle = ExampleSection:AddToggle({
    Title = "Colocar Tamanho Selecionado",
    Default = false,
    Callback = function(state)
        getgenv().AutoSize = state
        while getgenv().AutoSize do
            game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", selectedSize)
            task.wait(0.1)
        end
    end
})

local cameraTextbox = ExampleSection:AddTextbox({
    Title = "Camera Zoom",
    Placeholder = "Digitar Zoom...",
    Finished = true,
    Callback = function(v)
        local zoom = tonumber(v)
        if zoom then
            zoom = math.clamp(zoom, 0.5, 400)
            local player = game.Players.LocalPlayer
            player.CameraMinZoomDistance = zoom
            player.CameraMaxZoomDistance = zoom
        end
    end
})

local antiAFKEnabled = true
local antiAFKConnection

local function setupAntiAFK()
    if antiAFKConnection then return end
    local vu = game:GetService("VirtualUser")
    antiAFKConnection = game.Players.LocalPlayer.Idled:Connect(function()
        vu:Button2Down(Vector2.new(), workspace.CurrentCamera.CFrame)
        task.wait(1)
        vu:Button2Up(Vector2.new(), workspace.CurrentCamera.CFrame)
    end)
end

if antiAFKEnabled then
    setupAntiAFK()
end

local antiAFKToggle = ExampleSection:AddToggle({
    Title = "Não Cair pra Inatividade",
    Default = antiAFKEnabled,
    Callback = function(bool)
        antiAFKEnabled = bool
        if bool then
            setupAntiAFK()
        else
            if antiAFKConnection then
                antiAFKConnection:Disconnect()
                antiAFKConnection = nil
            end
        end
    end
})

local removePortalsButton = ExampleSection:AddButton({
    Title = "Remover Portal",
    Callback = function()
        for _, portal in pairs(game:GetDescendants()) do
            if portal.Name == "RobloxForwardPortals" then
                portal:Destroy()
            end
        end
        
        if _G.AdRemovalConnection then
            _G.AdRemovalConnection:Disconnect()
        end
        
        _G.AdRemovalConnection = game.DescendantAdded:Connect(function(descendant)
            if descendant.Name == "RobloxForwardPortals" then
                descendant:Destroy()
            end
        end)
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Anuncios Eliminados",
            Text = "Os Anuncios foram eliminados",
            Duration = 0
        })
    end
})

local timeDropdown = ExampleSection:AddDropdown({
    Title = "Mudar o Tempo",
    Values = {"Manhã", "Meio-Dia", "Tarde", "Pôr do Sol", "Noite", "Madrugada", "Alvorecer", "De Manhã Cedo", "Dia"},
    Default = "Dia",
    Callback = function(selection)
        local lighting = game:GetService("Lighting")
        lighting.Brightness = 2
        lighting.FogEnd = 100000
        lighting.Ambient = Color3.fromRGB(127,127,127)

        if selection == "Manhã" then
            lighting.ClockTime = 6
            lighting.Brightness = 2
            lighting.Ambient = Color3.fromRGB(200, 200, 255)
        elseif selection == "Meio-Dia" then
            lighting.ClockTime = 12
            lighting.Brightness = 3
            lighting.Ambient = Color3.fromRGB(255, 255, 255)
        elseif selection == "Tarde" then
            lighting.ClockTime = 16
            lighting.Brightness = 2.5
            lighting.Ambient = Color3.fromRGB(255, 220, 180)
        elseif selection == "Pôr do Sol" then
            lighting.ClockTime = 18
            lighting.Brightness = 2
            lighting.Ambient = Color3.fromRGB(255, 150, 100)
            lighting.FogEnd = 500
        elseif selection == "Noite" then
            lighting.ClockTime = 20
            lighting.Brightness = 1.5
            lighting.Ambient = Color3.fromRGB(100, 100, 150)
            lighting.FogEnd = 800
        elseif selection == "Madrugada" then
            lighting.ClockTime = 0
            lighting.Brightness = 1
            lighting.Ambient = Color3.fromRGB(50, 50, 100)
            lighting.FogEnd = 400
        elseif selection == "Alvorecer" then
            lighting.ClockTime = 4
            lighting.Brightness = 1.8
            lighting.Ambient = Color3.fromRGB(180, 180, 220)
        elseif selection == "De Manhã Cedo" then
            lighting.ClockTime = 2
            lighting.Brightness = 1.2
            lighting.Ambient = Color3.fromRGB(100, 120, 180)
        elseif selection == "Dia" then
            lighting.ClockTime = 12
            lighting.Brightness = 3
            lighting.Ambient = Color3.fromRGB(255, 255, 255)
        end
    end
})

local ExampleSection3 = tabAutomation:AddSection({
    Title = "Usável"
})

local autoWheelToggle = ExampleSection3:AddToggle({
    Title = "Girar Roleta Automaticamente",
    Default = false,
    Callback = function(Value)
        _G.autoFortuneWheelActive = Value
        if Value then
            local function openFortuneWheel()
                while _G.autoFortuneWheelActive do
                    local args = {
                        [1] = "openFortuneWheel",
                        [2] = game:GetService("ReplicatedStorage"):WaitForChild("fortuneWheelChances"):WaitForChild("Fortune Wheel")
                    }
                    game:GetService("ReplicatedStorage"):WaitForChild("rEvents"):WaitForChild("openFortuneWheelRemote"):InvokeServer(unpack(args))
                    wait(0)
                end
            end
            coroutine.wrap(openFortuneWheel)()
        else
            _G.autoFortuneWheelActive = false
        end
    end
})

local parts = {}
local partSize = 2048
local totalDistance = 50000
local startPosition = Vector3.new(-2, -9.5, -2)
local numberOfParts = math.ceil(totalDistance / partSize)

local function createParts()
    for x = 0, numberOfParts - 1 do
        for z = 0, numberOfParts - 1 do
            local newPartSide = Instance.new("Part")
            newPartSide.Size = Vector3.new(partSize, 1, partSize)
            newPartSide.Position = startPosition + Vector3.new(x * partSize, 0, z * partSize)
            newPartSide.Anchored = true
            newPartSide.Transparency = 1
            newPartSide.CanCollide = true
            newPartSide.Name = "Part_Side_" .. x .. "_" .. z
            newPartSide.Parent = workspace
            table.insert(parts, newPartSide)
            
            local newPartLeftRight = Instance.new("Part")
            newPartLeftRight.Size = Vector3.new(partSize, 1, partSize)
            newPartLeftRight.Position = startPosition + Vector3.new(-x * partSize, 0, z * partSize)
            newPartLeftRight.Anchored = true
            newPartLeftRight.Transparency = 1
            newPartLeftRight.CanCollide = true
            newPartLeftRight.Name = "Part_LeftRight_" .. x .. "_" .. z
            newPartLeftRight.Parent = workspace
            table.insert(parts, newPartLeftRight)
            
            local newPartUpLeft = Instance.new("Part")
            newPartUpLeft.Size = Vector3.new(partSize, 1, partSize)
            newPartUpLeft.Position = startPosition + Vector3.new(-x * partSize, 0, -z * partSize)
            newPartUpLeft.Anchored = true
            newPartUpLeft.Transparency = 1
            newPartUpLeft.CanCollide = true
            newPartUpLeft.Name = "Part_UpLeft_" .. x .. "_" .. z
            newPartUpLeft.Parent = workspace
            table.insert(parts, newPartUpLeft)
            
            local newPartUpRight = Instance.new("Part")
            newPartUpRight.Size = Vector3.new(partSize, 1, partSize)
            newPartUpRight.Position = startPosition + Vector3.new(x * partSize, 0, -z * partSize)
            newPartUpRight.Anchored = true
            newPartUpRight.Transparency = 1
            newPartUpRight.CanCollide = true
            newPartUpRight.Name = "Part_UpRight_" .. x .. "_" .. z
            newPartUpRight.Parent = workspace
            table.insert(parts, newPartUpRight)
        end
    end
end

local function makePartsWalkthrough()
    for _, part in ipairs(parts) do
        if part and part.Parent then
            part.CanCollide = false
        end
    end
end

local function makePartsSolid()
    for _, part in ipairs(parts) do
        if part and part.Parent then
            part.CanCollide = true
        end
    end
end

local waterToggle = ExampleSection3:AddToggle({
    Title = "Andar Na Água",
    Default = false,
    Callback = function(bool)
        if bool then
            createParts()
        else
            makePartsWalkthrough()
        end
    end
})

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local godModeToggle = false

local function autoJoinBrawl()
    while godModeToggle do
        pcall(function()
            ReplicatedStorage.rEvents.brawlEvent:FireServer("joinBrawl")
        end)
        task.wait(0.1)
    end
end

local godToggle = ExampleSection3:AddToggle({
    Title = "God Mode (Invencibilidade)",
    Default = false,
    Callback = function(state)
        godModeToggle = state
        if state then
            task.spawn(autoJoinBrawl)
        end
    end
})

player.CharacterAdded:Connect(function()
    if godModeToggle then
        task.spawn(autoJoinBrawl)
    end
end)

local tradeToggle = ExampleSection3:AddToggle({
    Title = "Desativar Trade",
    Default = false,
    Callback = function(State)
        if State then
            game:GetService("ReplicatedStorage").rEvents.tradingEvent:FireServer("disableTrading")
        else
            game:GetService("ReplicatedStorage").rEvents.tradingEvent:FireServer("enableTrading")
        end
    end
})

local infJumpEnabled = false
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

player.CharacterAdded:Connect(function(char)
    character = char
    humanoid = char:WaitForChild("Humanoid")
end)

game:GetService("UserInputService").JumpRequest:Connect(function()
    if infJumpEnabled and humanoid then
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

local infJumpToggle = ExampleSection3:AddToggle({
    Title = "Pular Infinitamente",
    Default = false,
    Callback = function(state)
        infJumpEnabled = state
    end
})

local autoEatBoostsEnabled = false
local boostsList = {
    "ULTRA Shake",
    "TOUGH Bar",
    "Protein Shake",
    "Energy Shake",
    "Protein Bar",
    "Energy Bar",
    "Tropical Shake"
}

local function eatAllBoosts()
    local player = game.Players.LocalPlayer
    local backpack = player:WaitForChild("Backpack")
    local character = player.Character or player.CharacterAdded:Wait()

    for _, boostName in ipairs(boostsList) do
        local boost = backpack:FindFirstChild(boostName)
        while boost do
            boost.Parent = character
            pcall(function()
                boost:Activate()
            end)
            task.wait(0)
            boost = backpack:FindFirstChild(boostName)
        end
    end
end

task.spawn(function()
    while true do
        if autoEatBoostsEnabled then
            eatAllBoosts()
            task.wait(2)
        else
            task.wait(1)
        end
    end
end)

local autoEatToggle = ExampleSection3:AddToggle({
    Title = "Limpar Inventário Automaticamente",
    Default = false,
    Callback = function(state)
        autoEatBoostsEnabled = state
    end
})

local antiKnockToggle = ExampleSection3:AddToggle({
    Title = "Não Ser Empurrado",
    Default = false,
    Callback = function(Value)
        if Value then
            local playerName = game.Players.LocalPlayer.Name
            local rootPart = game.Workspace:FindFirstChild(playerName):FindFirstChild("HumanoidRootPart")
            local bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.MaxForce = Vector3.new(100000, 0, 100000)
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            bodyVelocity.P = 1250
            bodyVelocity.Parent = rootPart
        else
            local playerName = game.Players.LocalPlayer.Name
            local rootPart = game.Workspace:FindFirstChild(playerName):FindFirstChild("HumanoidRootPart")
            local existingVelocity = rootPart:FindFirstChild("BodyVelocity")
            if existingVelocity and existingVelocity.MaxForce == Vector3.new(100000, 0, 100000) then
                existingVelocity:Destroy()
            end
        end
    end
})

local posLockToggle = ExampleSection3:AddToggle({
    Title = "Travar Personagem",
    Default = false,
    Callback = function(Value)
        if Value then
            local currentPos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
            getgenv().posLock = game:GetService("RunService").Heartbeat:Connect(function()
                if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = currentPos
                end
            end)
        else
            if getgenv().posLock then
                getgenv().posLock:Disconnect()
                getgenv().posLock = nil
            end
        end
    end
})

local infYieldButton = ExampleSection3:AddButton({
    Title = "Infinite Yield",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
        end)
    end
})

local shiftlockButton = ExampleSection3:AddButton({
    Title = "Shiftlock",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Shiftlock-22314"))()
        end)
    end
})

local executeBothButton = ExampleSection3:AddButton({
    Title = "Executar Os Dois",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
        end)
        pcall(function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Shiftlock-22314"))()
        end)
    end
})

local Renascimentos = Window:AddTab({
    Title = "Farmar",
    Icon = "scale"
})

local MainSection = Renascimentos:AddSection({
    Title = "Farmar Força"
})

MainSection:AddButton({
    Title = "Gamepass AutoLift",
    Callback = function()
        local gamepassFolder = game:GetService("ReplicatedStorage").gamepassIds
        local player = game:GetService("Players").LocalPlayer
        for _, gamepass in pairs(gamepassFolder:GetChildren()) do
            local value = Instance.new("IntValue")
            value.Name = gamepass.Name
            value.Value = gamepass.Value
            value.Parent = player.ownedGamepasses
        end
    end
})

local autoWeightSwitch = MainSection:AddToggle({
    Title = "Farmar com o Peso",
    Default = false,
    Callback = function(Value)
        _G.AutoWeight = Value
        if Value then
            local weightTool = game.Players.LocalPlayer.Backpack:FindFirstChild("Weight")
            if weightTool then
                game.Players.LocalPlayer.Character.Humanoid:EquipTool(weightTool)
            end
        else
            local character = game.Players.LocalPlayer.Character
            local equipped = character:FindFirstChild("Weight")
            if equipped then
                equipped.Parent = game.Players.LocalPlayer.Backpack
            end
        end
        task.spawn(function()
            while _G.AutoWeight do
                if not _G.AutoWeight then break end
                game:GetService("Players").LocalPlayer.muscleEvent:FireServer("rep")
                task.wait(0.1)
            end
        end)
    end
})

_G.AutoFlexoes = false
_G.ativoSocoRapido = false
_G.FlexoesRapidas = false
_G.SocoRapido = false
_G.autoFarm = false

local function equiparSoco()
    local jogador = game.Players.LocalPlayer
    local soco = jogador.Backpack:FindFirstChild("Punch")
    if soco and not jogador.Character:FindFirstChild("Punch") then
        if soco:FindFirstChild("attackTime") then
            soco.attackTime.Value = 0
        end
        jogador.Character.Humanoid:EquipTool(soco)
    end
end

local function equiparFlexoes()
    local jogador = game.Players.LocalPlayer
    local flexoes = jogador.Backpack:FindFirstChild("Pushups")
    if flexoes and not jogador.Character:FindFirstChild("Pushups") then
        if flexoes:FindFirstChild("repTime") then
            flexoes.repTime.Value = 0
        end
        jogador.Character.Humanoid:EquipTool(flexoes)
    end
end

local togglePedraFarmar = MainSection:AddToggle({
    Title = "Bater na Pedra e Farmar Automaticamente",
    Default = false,
    Callback = function(Valor)
        _G.AutoFlexoes = Valor
        _G.ativoSocoRapido = Valor
        _G.FlexoesRapidas = Valor
        _G.SocoRapido = Valor
        _G.autoFarm = Valor
        
        if Valor then
            equiparSoco()
            equiparFlexoes()
            
            task.spawn(function()
                while _G.autoFarm do
                    if not _G.autoFarm then break end
                    
                    equiparSoco()
                    equiparFlexoes()
                    
                    local jogador = game.Players.LocalPlayer
                    
                    jogador.muscleEvent:FireServer("punch", "rightHand")
                    jogador.muscleEvent:FireServer("punch", "leftHand")
                    
                    local personagem = jogador.Character
                    if personagem then
                        local ferramentaSoco = personagem:FindFirstChild("Punch")
                        if ferramentaSoco then
                            ferramentaSoco:Activate()
                        end
                    end
                    
                    jogador.muscleEvent:FireServer("rep")
                    
                    if jogador.Durability.Value >= 10000000 then
                        for _, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                            if v.Name == "neededDurability" and v.Value == 10000000 and 
                               personagem:FindFirstChild("LeftHand") and personagem:FindFirstChild("RightHand") then
                                firetouchinterest(v.Parent.Rock, personagem.RightHand, 0)
                                firetouchinterest(v.Parent.Rock, personagem.RightHand, 1)
                                firetouchinterest(v.Parent.Rock, personagem.LeftHand, 0)
                                firetouchinterest(v.Parent.Rock, personagem.LeftHand, 1)
                            end
                        end
                    end
                    
                    task.wait(0)
                end
            end)
        else
            local personagem = game.Players.LocalPlayer.Character
            local mochila = game.Players.LocalPlayer.Backpack
            
            local soco = personagem:FindFirstChild("Punch")
            if soco then
                soco.Parent = mochila
                if soco:FindFirstChild("attackTime") then
                    soco.attackTime.Value = 0.35
                end
            end
            
            local flexoes = personagem:FindFirstChild("Pushups")
            if flexoes then
                flexoes.Parent = mochila
                if flexoes:FindFirstChild("repTime") then
                    flexoes.repTime.Value = 1
                end
            end
        end
    end
})

local autoComerAtivo = false

local function comerOvoProteina()
    local jogador = game.Players.LocalPlayer
    local mochila = jogador:WaitForChild("Backpack")
    local personagem = jogador.Character or jogador.CharacterAdded:Wait()

    local ovo = mochila:FindFirstChild("Protein Egg")
    if ovo then
        ovo.Parent = personagem
        pcall(function()
            ovo:Activate()
        end)
    end
end

task.spawn(function()
    while true do
        if autoComerAtivo then
            comerOvoProteina()
            task.wait(1800)
        else
            task.wait(1)
        end
    end
end)

local autoEatEnabled = false

local function eatProteinEgg()
    local player = game.Players.LocalPlayer
    local backpack = player:WaitForChild("Backpack")
    local character = player.Character or player.CharacterAdded:Wait()
    local egg = backpack:FindFirstChild("Protein Egg")
    if egg then
        egg.Parent = character
        pcall(function()
            egg:Activate()
        end)
    end
end

task.spawn(function()
    while true do
        if autoEatEnabled then
            eatProteinEgg()
            task.wait(1800)
        else
            task.wait(1)
        end
    end
end)

MainSection:AddToggle({
    Title = "Comer Ovo Automaticamente",
    Default = false,
    Callback = function(state)
        autoEatEnabled = state
    end
})

MainSection:AddParagraph({
    Title = "Teleport",
    Content = ""
})

MainSection:AddButton({
    Title = "Ilha do Rei",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-8646, 17, -5738)
    end
})

local RenascimentosSection = Renascimentos:AddSection({
    Title = "Farmar Rebirth"
})

local valorObjetivoRenascimento = 0

local txtObjetivoRenascimento = RenascimentosSection:AddTextbox({
    Title = "Objetivo de Renascimento",
    Placeholder = "Digite o objetivo",
    Finished = true,
    Callback = function(texto)
        local novoValor = tonumber(texto)
        if novoValor and novoValor > 0 then
            valorObjetivoRenascimento = novoValor
            
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Objetivo Atualizado",
                Text = "Novo objetivo: " .. tostring(valorObjetivoRenascimento) .. " renascimentos",
                Duration = 3
            })
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Erro",
                Text = "Digite um valor maior que 0",
                Duration = 3
            })
        end
    end
})

local toggleObjetivoRenascimento = RenascimentosSection:AddToggle({
    Title = "Farmar até o Objetivo",
    Default = false,
    Callback = function(estado)
        _G.ativoObjetivoRenascimento = estado
        
        if estado then
            if _G.ativoRenascimentoInfinito and toggleRenascimentoInfinito then
                toggleRenascimentoInfinito:SetValue(false)
                _G.ativoRenascimentoInfinito = false
            end
            
            task.spawn(function()
                while _G.ativoObjetivoRenascimento and task.wait(0.1) do
                    local renascimentosAtuais = game.Players.LocalPlayer.leaderstats.Rebirths.Value
                    
                    if renascimentosAtuais >= valorObjetivoRenascimento then
                        toggleObjetivoRenascimento:SetValue(false)
                        _G.ativoObjetivoRenascimento = false
                        
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = "Objetivo Alcançado!",
                            Text = "Você alcançou " .. tostring(valorObjetivoRenascimento) .. " renascimentos",
                            Duration = 5
                        })
                        
                        break
                    end
                    
                    game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                end
            end)
        end
    end
})

local toggleRenascimentoInfinito = RenascimentosSection:AddToggle({
    Title = "Farmar Infinitamente",
    Default = false,
    Callback = function(estado)
        _G.ativoRenascimentoInfinito = estado
        
        if estado then
            if _G.ativoObjetivoRenascimento and toggleObjetivoRenascimento then
                toggleObjetivoRenascimento:SetValue(false)
                _G.ativoObjetivoRenascimento = false
            end
            
            task.spawn(function()
                while _G.ativoRenascimentoInfinito and task.wait(0.1) do
                    game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                end
            end)
        end
    end
})

local toggleTamanho1 = RenascimentosSection:AddToggle({
    Title = "Tamanho 1",
    Default = false,
    Callback = function(estado)
        _G.ativoTamanhoAuto = estado
        
        if estado then
            task.spawn(function()
                while _G.ativoTamanhoAuto and task.wait() do
                    game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 1)
                end
            end)
        end
    end
})

local toggleTeletransporteRei = RenascimentosSection:AddToggle({
    Title = "Teletransporte para o Rei",
    Default = false,
    Callback = function(estado)
        _G.ativoTeletransporte = estado
        
        if estado then
            task.spawn(function()
                while _G.ativoTeletransporte and task.wait() do
                    if game.Players.LocalPlayer.Character then
                        game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-8646, 17, -5738))
                    end
                end
            end)
        end
    end
})

local togglePesoAuto = RenascimentosSection:AddToggle({
    Title = "Farmar com o Peso",
    Default = false,
    Callback = function(Valor)
        _G.PesoAuto = Valor
        
        if Valor then
            local pesoFerramenta = game.Players.LocalPlayer.Backpack:FindFirstChild("Weight")
            if pesoFerramenta then
                game.Players.LocalPlayer.Character.Humanoid:EquipTool(pesoFerramenta)
            end
        else
            local personagem = game.Players.LocalPlayer.Character
            local equipado = personagem:FindFirstChild("Weight")
            if equipado then
                equipado.Parent = game.Players.LocalPlayer.Backpack
            end
        end
        
        task.spawn(function()
            while _G.PesoAuto do
                if not _G.PesoAuto then break end
                game:GetService("Players").LocalPlayer.muscleEvent:FireServer("rep")
                task.wait(0.1)
            end
        end)
    end
})

local toggleTravarPersonagem = RenascimentosSection:AddToggle({
    Title = "Travar Personagem",
    Default = false,
    Callback = function(Valor)
        if Valor then
            local posicaoAtual = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
            getgenv().travamentoPosicao = game:GetService("RunService").Heartbeat:Connect(function()
                if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = posicaoAtual
                end
            end)
        else
            if getgenv().travamentoPosicao then
                getgenv().travamentoPosicao:Disconnect()
                getgenv().travamentoPosicao = nil
            end
        end
    end
})

local FarmarPackTab = Window:AddTab({
    Title = "Farmar [Pack]",
    Icon = "activity"
})

local FarmarForcaSection = FarmarPackTab:AddSection({
    Title = "Farmar Força (Pack)"
})

local repsPerTick = 30
local runFastRep = false
local fastRebirth = false

local function getPing()
    local stats = game:GetService("Stats")
    local pingStat = stats:FindFirstChild("PerformanceStats") and stats.PerformanceStats:FindFirstChild("Ping")
    return pingStat and pingStat:GetValue() or 0
end

local function fastRepLoop()
    while runFastRep do
        local startTick = tick()
        while tick() - startTick < 0.75 and runFastRep do
            for i = 1, repsPerTick do
                game:GetService("Players").LocalPlayer.muscleEvent:FireServer("rep")
            end
            task.wait(0.02)
        end
        while runFastRep and getPing() >= 350 do
            task.wait(1)
        end
    end
end

FarmarForcaSection:AddToggle({
    Title = "Farmar Força Rapidamente (Precisa de 7-8 Packs)",
    Default = false,
    Callback = function(state)
        if state and not runFastRep then
            runFastRep = true
            task.spawn(fastRepLoop)
        elseif not state and runFastRep then
            runFastRep = false
        end
    end
})

local Player = game:GetService("Players").LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local function unequipPets()
    for _, folder in pairs(Player.petsFolder:GetChildren()) do
        if folder:IsA("Folder") then
            for _, pet in pairs(folder:GetChildren()) do
                ReplicatedStorage.rEvents.equipPetEvent:FireServer("unequipPet", pet)
            end
        end
    end
    task.wait(0.1)
end

local function equipPetsByName(name)
    unequipPets()
    task.wait(0.01)
    for _, pet in pairs(Player.petsFolder.Unique:GetChildren()) do
        if pet.Name == name then
            ReplicatedStorage.rEvents.equipPetEvent:FireServer("equipPet", pet)
        end
    end
end

local function activateProteinEgg()
    local tool = Player.Character:FindFirstChild("Protein Egg") or Player.Backpack:FindFirstChild("Protein Egg")
    if tool then
        game:GetService("Players").LocalPlayer.muscleEvent:FireServer("proteinEgg", tool)
    end
end

local running = false

task.spawn(function()
    while true do
        if running then
            activateProteinEgg()
            task.wait(1800)
        else
            task.wait(1)
        end
    end
end)

FarmarForcaSection:AddToggle({
    Title = "Comer Ovo Automaticamente",
    Default = false,
    Callback = function(state)
        running = state
        if state then
            activateProteinEgg()
        end
    end
})

local function activateShake()
    local tool = Player.Character:FindFirstChild("Tropical Shake") or Player.Backpack:FindFirstChild("Tropical Shake")
    if tool then
        game:GetService("Players").LocalPlayer.muscleEvent:FireServer("tropicalShake", tool)
    end
end

local runningShake = false

task.spawn(function()
    while true do
        if runningShake then
            activateShake()
            task.wait(900)
        else
            task.wait(1)
        end
    end
end)

FarmarForcaSection:AddToggle({
    Title = "Ocultar os Ganhos de Força (Evita Lag)",
    Default = false,
    Callback = function(bool)
        local rSto = game:GetService("ReplicatedStorage")
        for _, obj in pairs(rSto:GetChildren()) do
            if obj.Name:match("Frame$") then
                obj.Visible = not bool
            end
        end
    end
})

FarmarForcaSection:AddButton({
    Title = "Jungle Squat",
    Callback = function()
        local player = game.Players.LocalPlayer
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        hrp.CFrame = CFrame.new(-8371.43359375, 6.79806327, 2858.88525390)
        task.wait(0.2)
        game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.E, false, game)
        task.wait(0.05)
        game:GetService("VirtualInputManager"):SendKeyEvent(false, Enum.KeyCode.E, false, game)
    end
})

FarmarForcaSection:AddButton({
    Title = "Anti Lag",
    Callback = function()
        local jogador = game.Players.LocalPlayer
        local playerGui = jogador:WaitForChild("PlayerGui")
        local iluminacao = game:GetService("Lighting")

        for _, gui in pairs(playerGui:GetChildren()) do
            if gui:IsA("ScreenGui") then
                gui:Destroy()
            end
        end

        for _, v in pairs(iluminacao:GetChildren()) do
            if v:IsA("Sky") then
                v:Destroy()
            end
        end

        local ceuEscuro = Instance.new("Sky")
        ceuEscuro.Name = "DarkSky"
        ceuEscuro.SkyboxBk = "rbxassetid://0"
        ceuEscuro.SkyboxDn = "rbxassetid://0"
        ceuEscuro.SkyboxFt = "rbxassetid://0"
        ceuEscuro.SkyboxLf = "rbxassetid://0"
        ceuEscuro.SkyboxRt = "rbxassetid://0"
        ceuEscuro.SkyboxUp = "rbxassetid://0"
        ceuEscuro.Parent = iluminacao

        iluminacao.Brightness = 0
        iluminacao.ClockTime = 0
        iluminacao.TimeOfDay = "00:00:00"
        iluminacao.OutdoorAmbient = Color3.new(0, 0, 0)
        iluminacao.Ambient = Color3.new(0, 0, 0)
        iluminacao.FogColor = Color3.new(0, 0, 0)
        iluminacao.FogEnd = 100

        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("ParticleEmitter")
            or obj:IsA("PointLight")
            or obj:IsA("SpotLight")
            or obj:IsA("SurfaceLight") then
                obj:Destroy()
            end
        end
    end
})

FarmarForcaSection:AddButton({
    Title = "Equipar Swift Samurai",
    Callback = function()
        unequipPets()
        equipPetsByName("Swift Samurai")
    end
})

local FarmarSection = FarmarPackTab:AddSection({
    Title = "Farmar Rebirth (Pack)"
})

FarmarSection:AddToggle({
    Title = "Farmar Rebirth Rapidamente (Precisa de 7-8 Packs)",
    Default = false,
    Callback = function(bool)
        fastRebirth = bool
        if fastRebirth then
            task.spawn(function()
                local a = game:GetService("ReplicatedStorage")
                local c = game:GetService("Players").LocalPlayer

                local function unequipAllPets()
                    for _, folder in pairs(c.petsFolder:GetChildren()) do
                        if folder:IsA("Folder") then
                            for _, pet in pairs(folder:GetChildren()) do
                                a.rEvents.equipPetEvent:FireServer("unequipPet", pet)
                            end
                        end
                    end
                    task.wait(0.1)
                end

                local function equipPetByName(name)
                    unequipAllPets()
                    task.wait(0.01)
                    for _, pet in pairs(c.petsFolder.Unique:GetChildren()) do
                        if pet.Name == name then
                            a.rEvents.equipPetEvent:FireServer("equipPet", pet)
                        end
                    end
                end

                local function pressE()
                    local u = game:GetService("VirtualInputManager")
                    u:SendKeyEvent(true, "E", false, game)
                    task.wait(0.1)
                    u:SendKeyEvent(false, "E", false, game)
                end

                while fastRebirth do
                    local reb = c.leaderstats.Rebirths.Value
                    local neededStr = 10000 + (5000 * reb)

                    if c.ultimatesFolder:FindFirstChild("Golden Rebirth") then
                        neededStr = math.floor(neededStr * (1 - (c.ultimatesFolder["Golden Rebirth"].Value * 0.1)))
                    end

                    equipPetByName("Swift Samurai")

                    while c.leaderstats.Strength.Value < neededStr do
                        c.muscleEvent:FireServer("rep")
                        task.wait()
                    end

                    equipPetByName("Tribal Overlord")

                    local machine = workspace.machinesFolder:FindFirstChild("Jungle Bar Lift")
                    if machine and machine:FindFirstChild("interactSeat") then
                        c.Character.HumanoidRootPart.CFrame = machine.interactSeat.CFrame * CFrame.new(0, 3, 0)
                        repeat
                            task.wait(0.1)
                            pressE()
                        until c.Character.Humanoid.Sit
                    end

                    local before = c.leaderstats.Rebirths.Value
                    repeat
                        a.rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                        task.wait(0.1)
                    until c.leaderstats.Rebirths.Value > before
                end
            end)
        end
    end
})

FarmarSection:AddToggle({
    Title = "Tamanho 1",
    Default = false,
    Callback = function(state)
        getgenv().alwaysSize1 = state
        task.spawn(function()
            while getgenv().alwaysSize1 do
                game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 1)
                task.wait(0.5)
            end
        end)
    end
})

FarmarSection:AddToggle({
    Title = "Jungle Gym Lift",
    Default = false,
    Callback = function(state)
        if getgenv().working and not state then
            getgenv().working = false
            return
        end

        getgenv().working = state
        if state then
            local VIM = game:GetService("VirtualInputManager")
            local LocalPlayer = game:GetService("Players").LocalPlayer

            local function pressE()
                VIM:SendKeyEvent(true, "E", false, game)
                task.wait(0.1)
                VIM:SendKeyEvent(false, "E", false, game)
            end

            local function autoLift()
                while getgenv().working do
                    LocalPlayer.muscleEvent:FireServer("rep")
                    task.wait()
                end
            end

            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-8666, 34, 2070)
                task.wait(0.1)
                pressE()
                task.spawn(autoLift)
            end
        end
    end
})

FarmarSection:AddToggle({
    Title = "Ocultar os Ganhos de Força (Evita Lag)",
    Default = false,
    Callback = function(bool)
        local rSto = game:GetService("ReplicatedStorage")
        for _, obj in pairs(rSto:GetChildren()) do
            if obj.Name:match("Frame$") then
                obj.Visible = not bool
            end
        end
    end
})

FarmarSection:AddToggle({
    Title = "Travar Personagem",
    Default = false,
    Callback = function(Value)
        local LocalPlayer = game:GetService("Players").LocalPlayer

        if Value then
            local currentPos = LocalPlayer.Character.HumanoidRootPart.CFrame
            getgenv().posLock = game:GetService("RunService").Heartbeat:Connect(function()
                if LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = currentPos
                end
            end)
        else
            if getgenv().posLock then
                getgenv().posLock:Disconnect()
                getgenv().posLock = nil
            end
        end
    end
})

local Stats = Window:AddTab({
    Title = "Ver Estatísticas",
    Icon = "eye"
})

local StatsSection = Stats:AddSection({
    Title = "Estatisticas"
})

local SelectPlayerName = ""

local function getPlayerDropdownText(player)
    local cleanDisplayName = player.DisplayName:gsub("%s+", " "):gsub("^%s+", ""):gsub("%s+$", "")
    if cleanDisplayName == "" or cleanDisplayName == player.Name then
        return player.Name
    else
        return cleanDisplayName .. " | " .. player.Name
    end
end

local function updatePlayerDropdown()
    local players = game:GetService("Players"):GetPlayers()
    local playerValues = {}
    
    for _, player in ipairs(players) do
        if player ~= game.Players.LocalPlayer then
            table.insert(playerValues, getPlayerDropdownText(player))
        end
    end
    
    if PlayerDrop then
        PlayerDrop:SetValues(playerValues)
        
        if SelectPlayerName ~= "" then
            local playerStillExists = false
            for _, value in ipairs(playerValues) do
                if value:match("| (.+)") == SelectPlayerName or value == SelectPlayerName then
                    playerStillExists = true
                    break
                end
            end
            
            if not playerStillExists then
                SelectPlayerName = ""
            end
        end
    end
end

local initialPlayerValues = {}
local playersList = game:GetService("Players"):GetPlayers()
for _, player in ipairs(playersList) do
    if player ~= game.Players.LocalPlayer then
        table.insert(initialPlayerValues, getPlayerDropdownText(player))
    end
end

local PlayerDrop = StatsSection:AddDropdown({
    Title = "Selecionar Jogador",
    Values = initialPlayerValues,
    Default = "",
    Callback = function(Value)
        SelectPlayerName = Value:match("| (.+)") or Value
    end
})

game:GetService("Players").PlayerAdded:Connect(function()
    task.wait(0.1)
    updatePlayerDropdown()
end)

game:GetService("Players").PlayerRemoving:Connect(function(player)
    if player.Name == SelectPlayerName then
        SelectPlayerName = ""
    end
    task.wait(0.1)
    updatePlayerDropdown()
end)

local function FormatNumberWithCommas(number)
    local formatted = tostring(number):reverse():gsub("(%d%d%d)", "%1,"):reverse()
    return formatted:gsub("^,", "")
end

local function FormatAbbreviated(number)
    local abbreviations = {"", "K", "M", "B", "T", "Qa", "Qi"}
    local abbreviationIndex = 1
    while number >= 1000 and abbreviationIndex < #abbreviations do
        number = number / 1000
        abbreviationIndex = abbreviationIndex + 1
    end
    if number % 1 == 0 then
        return string.format("%.0f", number) .. abbreviations[abbreviationIndex]
    else
        return string.format("%.2f", number):gsub("%.?0+$", "") .. abbreviations[abbreviationIndex]
    end
end

local function FormatDisplay(value)
    local normal = ""
    if value % 1 == 0 then
        normal = FormatNumberWithCommas(math.floor(value))
    else
        normal = string.format("%.2f", value):gsub("%.?0+$", "")
    end
    local abbreviated = FormatAbbreviated(value)
    return " " .. normal .. " [ " .. abbreviated .. " ]"
end

local Update = StatsSection:AddParagraph({
    Title = "Força:",
    Content = ""
})

local Update1 = StatsSection:AddParagraph({
    Title = "Gemas:",
    Content = ""
})

local Update2 = StatsSection:AddParagraph({
    Title = "Renascimentos:",
    Content = ""
})

local Update3 = StatsSection:AddParagraph({
    Title = "Agilidade:",
    Content = ""
})

local Update4 = StatsSection:AddParagraph({
    Title = "Durabilidade:",
    Content = ""
})

local Update5 = StatsSection:AddParagraph({
    Title = "Quantas Pessoas Matou:",
    Content = ""
})

local Update6 = StatsSection:AddParagraph({
    Title = "Tempo na Ilha Do Rei:",
    Content = ""
})

local Update9 = StatsSection:AddParagraph({
    Title = "Atual Localização:",
    Content = ""
})

local Update10 = StatsSection:AddParagraph({
    Title = "Tamanho Salvo:",
    Content = ""
})

local Update11 = StatsSection:AddParagraph({
    Title = "Velocidade Salva:",
    Content = ""
})

local Update12 = StatsSection:AddParagraph({
    Title = "Karma Malígno:",
    Content = ""
})

local Update13 = StatsSection:AddParagraph({
    Title = "Bom Karma:",
    Content = ""
})

task.spawn(function()
    while task.wait(0.1) do
        if SelectPlayerName ~= "" then
            local player = game.Players:FindFirstChild(SelectPlayerName)
            if player then
                if player:FindFirstChild("Gems") then
                    Update1:SetContent(FormatDisplay(player.Gems.Value))
                else
                    Update1:SetContent("N/A")
                end
                if player:FindFirstChild("Agility") then
                    Update3:SetContent(FormatDisplay(player.Agility.Value))
                else
                    Update3:SetContent("N/A")
                end
                if player:FindFirstChild("Durability") then
                    Update4:SetContent(FormatDisplay(player.Durability.Value))
                else
                    Update4:SetContent("N/A")
                end
                if player:FindFirstChild("muscleKingTime") then
                    Update6:SetContent(FormatDisplay(player.muscleKingTime.Value))
                else
                    Update6:SetContent("N/A")
                end
                if player:FindFirstChild("customSize") then
                    Update10:SetContent(FormatDisplay(player.customSize.Value))
                else
                    Update10:SetContent("N/A")
                end
                if player:FindFirstChild("customSpeed") then
                    Update11:SetContent(FormatDisplay(player.customSpeed.Value))
                else
                    Update11:SetContent("N/A")
                end
                if player:FindFirstChild("evilKarma") then
                    Update12:SetContent(FormatDisplay(player.evilKarma.Value))
                else
                    Update12:SetContent("N/A")
                end
                if player:FindFirstChild("goodKarma") then
                    Update13:SetContent(FormatDisplay(player.goodKarma.Value))
                else
                    Update13:SetContent("N/A")
                end

                local leaderstats = player:FindFirstChild("leaderstats")
                if leaderstats then
                    if leaderstats:FindFirstChild("Strength") then
                        Update:SetContent(FormatDisplay(leaderstats.Strength.Value))
                    else
                        Update:SetContent("N/A")
                    end
                    if leaderstats:FindFirstChild("Rebirths") then
                        Update2:SetContent(FormatDisplay(leaderstats.Rebirths.Value))
                    else
                        Update2:SetContent("N/A")
                    end
                    if leaderstats:FindFirstChild("Kills") then
                        Update5:SetContent(FormatDisplay(leaderstats.Kills.Value))
                    else
                        Update5:SetContent("N/A")
                    end
                else
                    Update:SetContent("N/A")
                    Update2:SetContent("N/A")
                    Update5:SetContent("N/A")
                end

                if player:FindFirstChild("currentMap") then
                    Update9:SetContent(tostring(player.currentMap.Value))
                else
                    Update9:SetContent("N/A")
                end
            else
                Update:SetContent("")
                Update1:SetContent("")
                Update2:SetContent("")
                Update3:SetContent("")
                Update4:SetContent("")
                Update5:SetContent("")
                Update6:SetContent("")
                Update9:SetContent("")
                Update10:SetContent("")
                Update11:SetContent("")
                Update12:SetContent("")
                Update13:SetContent("")
            end
        else
            Update:SetContent("")
            Update1:SetContent("")
            Update2:SetContent("")
            Update3:SetContent("")
            Update4:SetContent("")
            Update5:SetContent("")
            Update6:SetContent("")
            Update9:SetContent("")
            Update10:SetContent("")
            Update11:SetContent("")
            Update12:SetContent("")
            Update13:SetContent("")
        end
    end
end)

local AdvancedStats = StatsSection:AddParagraph({
    Title = "Estatisticas Avançadas:",
    Content = ""
})

local enemyHealthLabel = StatsSection:AddParagraph({
    Title = "Durabilidade da Pessoa Selecionada:",
    Content = "N/A"
})

local playerDamageLabel = StatsSection:AddParagraph({
    Title = "Seu Dano:",
    Content = "N/A"
})

local hitsToKillLabel = StatsSection:AddParagraph({
    Title = "Quantidade de Socos Para Matar:",
    Content = "N/A"
})

local function calculateEnemyHealth(targetPlayer)
    if not targetPlayer then return 0 end
    local baseDura = 0
    local durabilityStat =
        targetPlayer:FindFirstChild("Durability") or
        (targetPlayer:FindFirstChild("leaderstats") and targetPlayer.leaderstats:FindFirstChild("Durability"))
    if durabilityStat then
        baseDura = durabilityStat.Value
    end
    local totalMultiplier = 1
    local ultFolder = targetPlayer:FindFirstChild("ultimatesFolder")
    if ultFolder then
        local infernalHealth = ultFolder:FindFirstChild("Infernal Health")
        if infernalHealth then
            totalMultiplier = totalMultiplier + 0.15 * (infernalHealth.Value or 0)
        end
    end
    local backpack = targetPlayer:FindFirstChild("Backpack")
    if backpack then
        local equippedPets = backpack:FindFirstChild("EquippedPets") or backpack:FindFirstChild("equippedPets")
        if equippedPets then
            for _, pet in ipairs(equippedPets:GetChildren()) do
                if string.lower(tostring(pet)):match("mighty") and string.lower(tostring(pet)):match("monster") then
                    totalMultiplier = totalMultiplier + 0.5
                    break
                end
            end
        end
    end
    return baseDura * totalMultiplier
end

local function calculateLocalPlayerDamage()
    local LocalPlayer = game.Players.LocalPlayer
    if not LocalPlayer then return 0 end
    local strengthStat = nil
    local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
    if leaderstats then
        strengthStat = leaderstats:FindFirstChild("Strength")
    end
    if not strengthStat then return 0 end
    local baseDamage = strengthStat.Value * 0.0667
    local totalMultiplier = 1
    local ultFolder = LocalPlayer:FindFirstChild("ultimatesFolder")
    if ultFolder then
        local demonDamage = ultFolder:FindFirstChild("Demon Damage")
        if demonDamage then
            totalMultiplier = totalMultiplier + math.min(0.1 * (demonDamage.Value or 0), 0.5)
        end
    end
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if backpack then
        local equippedPets = backpack:FindFirstChild("EquippedPets") or backpack:FindFirstChild("equippedPets")
        if equippedPets then
            for _, pet in ipairs(equippedPets:GetChildren()) do
                if string.lower(tostring(pet)):match("wild") and string.lower(tostring(pet)):match("wizard") then
                    totalMultiplier = totalMultiplier + 0.5
                    break
                end
            end
        end
    end
    return baseDamage * totalMultiplier
end

local function calculateHitsToKill(health, damage)
    if damage <= 0 then return "∞" end
    local hits = math.ceil(health / damage)
    if hits > 50 then
        return "∞"
    elseif hits < 1 then
        return 1
    else
        return hits
    end
end

local function updateAdvancedStats(targetPlayer)
    if not targetPlayer then
        enemyHealthLabel:SetContent("N/A")
        playerDamageLabel:SetContent("N/A")
        hitsToKillLabel:SetContent("N/A")
        return
    end
    local enemyHealth = calculateEnemyHealth(targetPlayer)
    local playerDamage = calculateLocalPlayerDamage()
    local hitsToKill = calculateHitsToKill(enemyHealth, playerDamage)
    enemyHealthLabel:SetContent(FormatDisplay(enemyHealth))
    playerDamageLabel:SetContent(FormatDisplay(playerDamage))
    hitsToKillLabel:SetContent(tostring(hitsToKill))
end

task.spawn(function()
    while task.wait(0.3) do
        local selectedPlayer = game.Players:FindFirstChild(SelectPlayerName)
        updateAdvancedStats(selectedPlayer)
    end
end)

spectating = false
currentConnection = nil
spectateLoop = nil

function setSpectate(plr)
    if currentConnection then
        currentConnection:Disconnect()
        currentConnection = nil
    end

    local function updateCamera()
        if spectating and plr and plr.Parent then
            local humanoid = plr.Character and plr.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                workspace.CurrentCamera.CameraSubject = humanoid
            end
        end
    end

    currentConnection = plr.CharacterAdded:Connect(function(char)
        local hum = char:WaitForChild("Humanoid")
        workspace.CurrentCamera.CameraSubject = hum
    end)

    if spectateLoop then
        spectateLoop:Disconnect()
    end
    spectateLoop = game:GetService("RunService").Heartbeat:Connect(updateCamera)

    updateCamera()
end

StatsSection:AddToggle({
    Title = "Olhar Pessoa Selecionada",
    Default = false,
    Callback = function(val)
        local lp = game.Players.LocalPlayer
        if val then
            spectating = true
            if SelectPlayerName then
                local plr = game.Players:FindFirstChild(SelectPlayerName)
                if plr then
                    setSpectate(plr)
                end
            end
        else
            spectating = false
            if currentConnection then
                currentConnection:Disconnect()
                currentConnection = nil
            end
            if spectateLoop then
                spectateLoop:Disconnect()
                spectateLoop = nil
            end
            if lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") then
                workspace.CurrentCamera.CameraSubject = lp.Character:FindFirstChildOfClass("Humanoid")
            end
        end
    end
})

local Matar = Window:AddTab({
    Title = "Matar",
    Icon = "skull"
})

local ConfiguracaoSection = Matar:AddSection({
    Title = "Configurações"
})

local JogadorLocal = game.Players.LocalPlayer
local Jogadores = game:GetService("Players")

local function obterTextoJogador(jogador)
    local nomeLimpo = jogador.DisplayName:gsub("%s+", " "):gsub("^%s+", ""):gsub("%s+$", "")
    if nomeLimpo == "" or nomeLimpo == jogador.Name then
        return jogador.Name
    else
        return nomeLimpo .. " | " .. jogador.Name
    end
end

_G.jogadoresListaBranca = _G.jogadoresListaBranca or {}
_G.jogadoresListaPreta = _G.jogadoresListaPreta or {}

local function estaNaListaBranca(jogador)
    for _, nome in ipairs(_G.jogadoresListaBranca) do
        if nome:lower() == jogador.Name:lower() then
            return true
        end
    end
    return false
end

local function estaNaListaPreta(jogador)
    for _, nome in ipairs(_G.jogadoresListaPreta) do
        if nome:lower() == jogador.Name:lower() then
            return true
        end
    end
    return false
end

local dropdownPet = ConfiguracaoSection:AddDropdown({
    Title = "Selecionar Pet",
    Values = {"Wild Wizard", "Mighty Monster"},
    Default = "",
    Callback = function(texto)
        local petsFolder = JogadorLocal.petsFolder
        for _, pasta in pairs(petsFolder:GetChildren()) do
            if pasta:IsA("Folder") then
                for _, pet in pairs(pasta:GetChildren()) do
                    game:GetService("ReplicatedStorage").rEvents.equipPetEvent:FireServer("unequipPet", pet)
                end
            end
        end
        task.wait(0.2)

        local nomePet = texto
        local petsParaEquipar = {}

        for _, pet in pairs(JogadorLocal.petsFolder.Unique:GetChildren()) do
            if pet.Name == nomePet then
                table.insert(petsParaEquipar, pet)
            end
        end

        for i = 1, math.min(8, #petsParaEquipar) do
            game:GetService("ReplicatedStorage").rEvents.equipPetEvent:FireServer("equipPet", petsParaEquipar[i])
            task.wait(0.1)
        end
    end
})

local toggleSemAnimacao = ConfiguracaoSection:AddToggle({
    Title = "Soco Sem Animação",
    Default = false,
    Callback = function(estado)
        if estado then
            local animacoesBloqueadas = {
                ["rbxassetid://3638729053"] = true,
                ["rbxassetid://3638767427"] = true,
            }

            local function pararAnimacoesBloqueadas(humanoide)
                for _, track in pairs(humanoide:GetPlayingAnimationTracks()) do
                    if track.Animation then
                        local animId = track.Animation.AnimationId
                        local animNome = track.Name:lower()
                        if animacoesBloqueadas[animId] or animNome:match("punch") or animNome:match("attack") or animNome:match("right") then
                            track:Stop()
                        end
                    end
                end
            end

            local function configurarBloqueioAnimacao()
                local char = JogadorLocal.Character
                if not char or not char:FindFirstChild("Humanoid") then return end
                local humanoide = char:FindFirstChild("Humanoid")

                pararAnimacoesBloqueadas(humanoide)

                if not _G.ConexaoAnimBloqueada then
                    _G.ConexaoAnimBloqueada = humanoide.AnimationPlayed:Connect(function(track)
                        if track.Animation then
                            local animId = track.Animation.AnimationId
                            local animNome = track.Name:lower()
                            if animacoesBloqueadas[animId] or animNome:match("punch") or animNome:match("attack") or animNome:match("right") then
                                track:Stop()
                            end
                        end
                    end)
                end
            end

            configurarBloqueioAnimacao()

            _G.LoopAutoSoco = true
            task.spawn(function()
                while _G.LoopAutoSoco do
                    local char = JogadorLocal.Character
                    if char and char:FindFirstChild("Humanoid") then
                        pararAnimacoesBloqueadas(char.Humanoid)
                    end

                    local soco = JogadorLocal.Backpack:FindFirstChild("Punch") or (char and char:FindFirstChild("Punch"))
                    if soco then
                        if soco.Parent ~= char then
                            soco.Parent = char
                        end
                        JogadorLocal.muscleEvent:FireServer("punch", "rightHand")
                        JogadorLocal.muscleEvent:FireServer("punch", "leftHand")
                    end
                    task.wait(0.01)
                end
            end)

            if not _G.ConexaoPersonagemAdicionado then
                _G.ConexaoPersonagemAdicionado = JogadorLocal.CharacterAdded:Connect(function()
                    task.wait(1)
                    configurarBloqueioAnimacao()
                end)
            end
        else
            _G.LoopAutoSoco = false
            if _G.ConexaoAnimBloqueada then _G.ConexaoAnimBloqueada:Disconnect() _G.ConexaoAnimBloqueada = nil end
        end
    end
})

local function desabilitarOvoProteina()
    pcall(function()
        local mochila = JogadorLocal:WaitForChild("Backpack")
        local ovoProteina = mochila:FindFirstChild("Protein Egg")
        
        if ovoProteina then
            if ovoProteina.Enabled == false and ovoProteina.CanBeDropped == false then
                return
            else
                ovoProteina:Destroy()
                task.wait(0.1)
            end
        end
    
        local novoOvo = Instance.new("Tool")
        novoOvo.Name = "Protein Egg"
        novoOvo.Enabled = false
        novoOvo.CanBeDropped = false
        novoOvo.RequiresHandle = false
        novoOvo.Parent = mochila
    end)
end

local function monitorarOvoProteina()
    while true do
        desabilitarOvoProteina()
        task.wait(0.5)
    end
end

JogadorLocal.CharacterAdded:Connect(function()
    task.wait(1)
    desabilitarOvoProteina()
end)

local botaoDesabilitarOvo = ConfiguracaoSection:AddButton({
    Title = "Desabilitar Ovo de Proteína",
    Callback = function()
        desabilitarOvoProteina()
        coroutine.wrap(monitorarOvoProteina)()
    end
})

desabilitarOvoProteina()
coroutine.wrap(monitorarOvoProteina)()

local function verificarPersonagem()
    if not JogadorLocal.Character then
        repeat task.wait() until JogadorLocal.Character
    end
    return JogadorLocal.Character
end

local function pegarFerramenta()
    for _, v in pairs(JogadorLocal.Backpack:GetChildren()) do
        if v.Name == "Punch" and JogadorLocal.Character:FindFirstChild("Humanoid") then
            JogadorLocal.Character.Humanoid:EquipTool(v)
        end
    end
    JogadorLocal.muscleEvent:FireServer("punch", "leftHand")
    JogadorLocal.muscleEvent:FireServer("punch", "rightHand")
end

local function jogadorEstaVivo(jogador)
    return jogador and jogador.Character
       and jogador.Character:FindFirstChild("HumanoidRootPart")
       and jogador.Character:FindFirstChild("Humanoid")
       and jogador.Character.Humanoid.Health > 0
end

local function matarJogador(alvo)
    if not jogadorEstaVivo(alvo) then return end
    local personagem = verificarPersonagem()
    if personagem and personagem:FindFirstChild("LeftHand") then
        pcall(function()
            firetouchinterest(alvo.Character.HumanoidRootPart, personagem.LeftHand, 0)
            firetouchinterest(alvo.Character.HumanoidRootPart, personagem.LeftHand, 1)
            pegarFerramenta()
        end)
    end
end

local function atualizarDropdownListaBranca()
    local jogadoresLista = Jogadores:GetPlayers()
    local valoresDropdown = {}
    
    for _, jogador in ipairs(jogadoresLista) do
        if jogador ~= JogadorLocal then
            table.insert(valoresDropdown, obterTextoJogador(jogador))
        end
    end
    
    if dropdownListaBranca then
        dropdownListaBranca:SetValues(valoresDropdown)
    end
end

local function atualizarDropdownListaPreta()
    local jogadoresLista = Jogadores:GetPlayers()
    local valoresDropdown = {}
    
    for _, jogador in ipairs(jogadoresLista) do
        if jogador ~= JogadorLocal then
            table.insert(valoresDropdown, obterTextoJogador(jogador))
        end
    end
    
    if dropdownListaPreta then
        dropdownListaPreta:SetValues(valoresDropdown)
    end
end

local valoresIniciaisListaBranca = {}
local valoresIniciaisListaPreta = {}
for _, jogador in ipairs(Jogadores:GetPlayers()) do
    if jogador ~= JogadorLocal then
        table.insert(valoresIniciaisListaBranca, obterTextoJogador(jogador))
        table.insert(valoresIniciaisListaPreta, obterTextoJogador(jogador))
    end
end

local dropdownListaBranca = ConfiguracaoSection:AddDropdown({
    Title = "Selecionar Pessoa(s) Para Tirar do Matar Todos",
    Values = valoresIniciaisListaBranca,
    Default = "",
    Callback = function(textoSelecionado)
        local nomeJogador = textoSelecionado:match("| (.+)$") or textoSelecionado
        if nomeJogador then
            nomeJogador = nomeJogador:match("^%s*(.-)%s*$")
            for _, nome in ipairs(_G.jogadoresListaBranca) do
                if nome:lower() == nomeJogador:lower() then return end
            end
            table.insert(_G.jogadoresListaBranca, nomeJogador)
        end
    end
})

local dropdownListaPreta = ConfiguracaoSection:AddDropdown({
    Title = "Selecionar Pessoa(s) Para Matar",
    Values = valoresIniciaisListaPreta,
    Default = "",
    Callback = function(textoSelecionado)
        local nomeJogador = textoSelecionado:match("| (.+)$") or textoSelecionado
        if nomeJogador then
            nomeJogador = nomeJogador:match("^%s*(.-)%s*$")
            for _, nome in ipairs(_G.jogadoresListaPreta) do
                if nome:lower() == nomeJogador:lower() then return end
            end
            table.insert(_G.jogadoresListaPreta, nomeJogador)
        end
    end
})

local function atualizarTodosDropdowns()
    atualizarDropdownListaBranca()
    atualizarDropdownListaPreta()
end

Jogadores.PlayerAdded:Connect(function()
    task.wait(0.1)
    atualizarTodosDropdowns()
end)

Jogadores.PlayerRemoving:Connect(function()
    task.wait(0.1)
    atualizarTodosDropdowns()
end)

local botaoLimparListaBranca = ConfiguracaoSection:AddButton({
    Title = "Voltar a Matar Quem Você Tirou do Matar Todos",
    Callback = function()
        _G.jogadoresListaBranca = {}
    end
})

local botaoLimparListaPreta = ConfiguracaoSection:AddButton({
    Title = "Parar de Matar Quem Você Selecionou",
    Callback = function()
        _G.jogadoresListaPreta = {}
    end
})

local toggleAmigosListaBranca = ConfiguracaoSection:AddToggle({
    Title = "Remover Amigos do Matar Todos Automaticamente",
    Default = false,
    Callback = function(estado)
        if estado then
            for _, jogador in pairs(Jogadores:GetPlayers()) do
                if jogador ~= JogadorLocal and jogador:IsFriendsWith(JogadorLocal.UserId) then
                    local jaEsta = false
                    for _, nome in ipairs(_G.jogadoresListaBranca) do
                        if nome:lower() == jogador.Name:lower() then
                            jaEsta = true
                            break
                        end
                    end
                    if not jaEsta then
                        table.insert(_G.jogadoresListaBranca, jogador.Name)
                    end
                end
            end
        end
    end
})

local autoEquipPunch = false
local autoPunchNoAnim = false

local autoEquipToggle = ConfiguracaoSection:AddToggle({
    Title = "Equipar Soco Automaticamente",
    Default = false,
    Callback = function(state)
        autoEquipPunch = state
        task.spawn(function()
            while autoEquipPunch do
                local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                if punch then
                    punch.Parent = LocalPlayer.Character
                end
                task.wait(0.1)
            end
        end)
    end
})

local autoPunchNoAnimToggle = ConfiguracaoSection:AddToggle({
    Title = "Bater [Sem Animação]",
    Default = false,
    Callback = function(state)
        autoPunchNoAnim = state
        task.spawn(function()
            while autoPunchNoAnim do
                local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
                if punch then
                    if punch.Parent ~= LocalPlayer.Character then
                        punch.Parent = LocalPlayer.Character
                    end
                    LocalPlayer.muscleEvent:FireServer("punch", "rightHand")
                    LocalPlayer.muscleEvent:FireServer("punch", "leftHand")
                else
                    autoPunchNoAnim = false
                end
                task.wait(0.01)
            end
        end)
    end
})

local fastPunchToggle = ConfiguracaoSection:AddToggle({
    Title = "Bater Rapidamente",
    Default = false,
    Callback = function(state)
        _G.fastHitActive = state
        if state then
            task.spawn(function()
                while _G.fastHitActive do
                    local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                    if punch then
                        punch.Parent = LocalPlayer.Character
                        if punch:FindFirstChild("attackTime") then
                            punch.attackTime.Value = 0
                        end
                    end
                    task.wait(0.1)
                end
            end)
            task.spawn(function()
                while _G.fastHitActive do
                    local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
                    if punch then
                        punch:Activate()
                    end
                    task.wait(0.1)
                end
            end)
        else
            local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
            if punch then
                punch.Parent = LocalPlayer.Backpack
            end
        end
    end
})

local ultraFastPunchToggle = ConfiguracaoSection:AddToggle({
    Title = "Bater Mega Rapidamente",
    Default = false,
    Callback = function(state)
        _G.autoPunchActive = state
        if state then
            task.spawn(function()
                while _G.autoPunchActive do
                    local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                    if punch then
                        punch.Parent = LocalPlayer.Character
                        if punch:FindFirstChild("attackTime") then
                            punch.attackTime.Value = 0
                        end
                    end
                    task.wait()
                end
            end)
            task.spawn(function()
                while _G.autoPunchActive do
                    local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
                    if punch then
                        punch:Activate()
                    end
                    task.wait()
                end
            end)
        else
            local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
            if punch then
                punch.Parent = LocalPlayer.Backpack
            end
        end
    end
})

local toggleMatarLista = ConfiguracaoSection:AddToggle({
    Title = "Matar Pessoas Seleciondas",
    Default = false,
    Callback = function(estado)
        _G.matarApenasListaPreta = estado
        if estado then
            if not _G.conexaoMatarLista then
                _G.conexaoMatarLista = game:GetService("RunService").Heartbeat:Connect(function()
                    for _, jogador in ipairs(Jogadores:GetPlayers()) do
                        if jogador ~= JogadorLocal and estaNaListaPreta(jogador) then
                            matarJogador(jogador)
                        end
                    end
                end)
            end
        else
            if _G.conexaoMatarLista then
                _G.conexaoMatarLista:Disconnect()
                _G.conexaoMatarLista = nil
            end
        end
    end
})

local toggleMatarTodos = ConfiguracaoSection:AddToggle({
    Title = "Matar Todos",
    Default = false,
    Callback = function(estado)
        _G.matarTodos = estado
        if estado then
            if not _G.conexaoMatarTodos then
                _G.conexaoMatarTodos = game:GetService("RunService").Heartbeat:Connect(function()
                    for _, jogador in ipairs(Jogadores:GetPlayers()) do
                        if jogador ~= JogadorLocal and not estaNaListaBranca(jogador) then
                            matarJogador(jogador)
                        end
                    end
                end)
            end
        else
            if _G.conexaoMatarTodos then
                _G.conexaoMatarTodos:Disconnect()
                _G.conexaoMatarTodos = nil
            end
        end
    end
})

local txtAlcance = ConfiguracaoSection:AddTextbox({
    Title = "Tamanho do Matar a Longa Distância (1-140)",
    Placeholder = "Digite o alcance",
    Default = "20",
    Finished = true,
    Callback = function(texto)
        local alcance = tonumber(texto)
        if alcance then
            _G.alcanceAnelMorte = math.clamp(alcance, 1, 140)
        end
    end
})

local toggleAnelMorte = ConfiguracaoSection:AddToggle({
    Title = "Matar a Longa Distância",
    Default = false,
    Callback = function(estado)
        _G.anelMorteAtivo = estado
        if estado then
            if not _G.conexaoAnelMorte then
                _G.conexaoAnelMorte = game:GetService("RunService").Heartbeat:Connect(function()
                    local personagem = verificarPersonagem()
                    local minhaPos = personagem and personagem:FindFirstChild("HumanoidRootPart") and personagem.HumanoidRootPart.Position
                    if minhaPos then
                        for _, jogador in ipairs(Jogadores:GetPlayers()) do
                            if jogador ~= JogadorLocal and not estaNaListaBranca(jogador) and jogadorEstaVivo(jogador) then
                                if (minhaPos - jogador.Character.HumanoidRootPart.Position).Magnitude <= (_G.alcanceAnelMorte or 20) then
                                    matarJogador(jogador)
                                end
                            end
                        end
                    end
                end)
            end
        else
            if _G.conexaoAnelMorte then
                _G.conexaoAnelMorte:Disconnect()
                _G.conexaoAnelMorte = nil
            end
        end
    end
})

local labelListaBranca = ConfiguracaoSection:AddParagraph({
    Title = "Pessoas Que Você Tirou do Matar Todos:",
    Content = "Nenhum"
})

local labelListaPreta = ConfiguracaoSection:AddParagraph({
    Title = "Pessoas que Você Está Matando:",
    Content = "Nenhum"
})

local function atualizarLabelsListas()
    if #_G.jogadoresListaBranca == 0 then
        labelListaBranca:SetContent("Nenhum")
    else
        local texto = ""
        for i, nome in ipairs(_G.jogadoresListaBranca) do
            if i > 3 then
                texto = texto .. "..."
                break
            end
            if i > 1 then texto = texto .. "\n" end
            texto = texto .. nome
        end
        labelListaBranca:SetContent(texto)
    end

    if #_G.jogadoresListaPreta == 0 then
        labelListaPreta:SetContent("Nenhum")
    else
        local texto = ""
        for i, nome in ipairs(_G.jogadoresListaPreta) do
            if i > 3 then
                texto = texto .. "..."
                break
            end
            if i > 1 then texto = texto .. "\n" end
            texto = texto .. nome
        end
        labelListaPreta:SetContent(texto)
    end
end

local ultimoContadorBranca = 0
local ultimoContadorPreta = 0

task.spawn(function()
    while true do
        if #_G.jogadoresListaBranca ~= ultimoContadorBranca or #_G.jogadoresListaPreta ~= ultimoContadorPreta then
            atualizarLabelsListas()
            ultimoContadorBranca = #_G.jogadoresListaBranca
            ultimoContadorPreta = #_G.jogadoresListaPreta
        end
        task.wait(0.2)
    end
end)

local TabPets = Window:AddTab({
    Title = "Comprar Pets",
    Icon = "layers"
})

local PetsSection = TabPets:AddSection({
    Title = "Comprar Pets"
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local selectedPet = "Neon Guardian"
local petDropdown = PetsSection:AddDropdown({
    Title = "Selecionar Pet",
    Values = {
        "Neon Guardian", "Blue Birdie", "Blue Bunny", "Blue Firecaster", "Blue Pheonix", "Crimson Falcon",
        "Cybernetic Showdown Dragon", "Dark Golem", "Dark Legends Manticore", "Dark Vampy", "Darkstar Hunter",
        "Eternal Strike Leviathan", "Frostwave Legends Penguin", "Gold Warrior", "Golden Pheonix", "Golden Viking",
        "Green Butterfly", "Green Firecaster", "Infernal Dragon", "Lightning Strike Phantom", "Magic Butterfly",
        "Muscle Sensei", "Orange Hedgehog", "Orange Pegasus", "Phantom Genesis Dragon", "Purple Dragon",
        "Purple Falcon", "Red Dragon", "Red Firecaster", "Red Kitty", "Silver Dog", "Ultimate Supernova Pegasus",
        "Ultra Birdie", "White Pegasus", "White Pheonix", "Yellow Butterfly"
    },
    Default = "Neon Guardian",
    Callback = function(selecao)
        selectedPet = selecao
    end
})

PetsSection:AddToggle({
    Title = "Comprar Pet Automaticamente",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchPet = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchPet and selectedPet ~= "" do
                    local petToOpen = ReplicatedStorage.cPetShopFolder:FindFirstChild(selectedPet)
                    if petToOpen then
                        ReplicatedStorage.cPetShopRemote:InvokeServer(petToOpen)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local selectedAura = "Blue Aura"
local auraDropdown = PetsSection:AddDropdown({
    Title = "Selecionar Aura",
    Values = {
        "Astral Electro", "Azure Tundra", "Blue Aura", "Dark Electro", "Dark Lightning", "Dark Storm",
        "Electro", "Enchanted Mirage", "Entropic Blast", "Eternal Megastrike", "Grand Supernova",
        "Green Aura", "Inferno", "Lightning", "Muscle King", "Power Lightning", "Purple Aura",
        "Purple Nova", "Red Aura", "Supernova", "Ultra Inferno", "Ultra Mirage", "Unstable Mirage",
        "Yellow Aura"
    },
    Default = "Blue Aura",
    Callback = function(selecao)
        selectedAura = selecao
    end
})

PetsSection:AddToggle({
    Title = "Comprar Aura Automaticamente",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchAura = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchAura and selectedAura ~= "" do
                    local auraToOpen = ReplicatedStorage.cPetShopFolder:FindFirstChild(selectedAura)
                    if auraToOpen then
                        ReplicatedStorage.cPetShopRemote:InvokeServer(auraToOpen)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local TabTeleport = Window:AddTab({
    Title = "Teleport",
    Icon = "map-pin"
})

local TeleportSection = TabTeleport:AddSection({
    Title = "Locais de Teleport"
})

TeleportSection:AddButton({
    Title = "Inicio",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(2, 8, 115)
    end
})

TeleportSection:AddButton({
    Title = "Ilha Secreta",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(1947, 2, 6191)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de Menos 10k Forca",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-34, 7, 1903)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de 1 Rebirth",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-2600.00244, 3.67686558, -403.884369, 0.0873617008, 1.0482899e-09, 0.99617666, 3.07204253e-08, 1, -3.7464023e-09, -0.99617666, 3.09302628e-08, 0.0873617008)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de 5 Rebirths",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(2255, 7, 1071)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de 15 Rebirths",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-6768, 7, -1287)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de 30 Rebirths",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(4604, 991, -3887)
    end
})

TeleportSection:AddButton({
    Title = "Ilha do Rei",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-8646, 17, -5738)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de 60 Rebirths",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-8659, 6, 2384)
    end
})

TeleportSection:AddButton({
    Title = "Ilha de Lava [Brawl]",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(4471, 119, -8836)
    end
})

TeleportSection:AddButton({
    Title = "Ilha do Deserto [Brawl]",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(960, 17, -7398)
    end
})

TeleportSection:AddButton({
    Title = "Ilha do Ringue [Brawl]",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        humanoidRootPart.CFrame = CFrame.new(-1849, 20, -6335)
    end
})

local CreditosTab = Window:AddTab({
    Title = "Créditos",
    Icon = "heart"
})

local CreditosSection = CreditosTab:AddSection({
    Title = "Informações"
})

CreditosSection:AddParagraph({
    Title = "FusionX Free Version",
    Content = "Feito por Sad."
})

CreditosSection:AddParagraph({
    Title = "Tem alguma dúvida ou encontrou algum problema?",
    Content = "Entre No Servidor do Discord!"
})

CreditosSection:AddParagraph({
    Title = "Mais atualizações em breve.",
    Content = "Aproveite!"
})

CreditosSection:AddParagraph({
    Title = "Discord",
    Content = "Junte-se ao nosso servidor!"
})

CreditosSection:AddButton({
    Title = "Copiar Link do Discord",
    Callback = function()
        local discordLink = "https://discord.gg/TCsaTKQ66q"
        pcall(function()
            if setclipboard then
                setclipboard(discordLink)
            end
        end)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Discord",
                Text = "Link copiado! " .. discordLink,
                Duration = 5
            })
        end)
    end
})
